Bonjour <%= @resource.name %>!

Quelqu'un a demandé un lien pour changer votre mot de passe sur <%= Gitlab.config.gitlab.url %>, 
et vous pouvez le faire via le lien ci-dessous.

<%= edit_password_url(@resource, reset_password_token: @token) %>

Si vous n'avez pas fait cette demande, ignorez cet email.
Votre mot de passe ne changera pas jusqu'à ce que vous accédiez au lien ci-dessus et 
que vous en créassiez un nouveau.

----------------------------------------

Hello, <%= @resource.name %>!

Someone, hopefully you, has requested to reset the password for your GitLab
account on <%= Gitlab.config.gitlab.url %>

If you did not perform this request, you can safely ignore this email.

Otherwise, click the link below to complete the process:

<%= edit_password_url(@resource, reset_password_token: @token) %>
