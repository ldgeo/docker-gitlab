Bonjour <%= @resource.name %>!

<% if @resource.unconfirmed_email.present? %>
Vous pouvez confirmer votre email (<%= @resource.unconfirmed_email %>) via le lien ci-dessous:
<% else %>
  Vous pouvez confirmer votre email via le lien ci-dessous:
<% end %>

<%= confirmation_url(@resource, confirmation_token: @token) %>

--------------------------------------------

Welcome <%= @resource.name %>!

<% if @resource.unconfirmed_email.present? %>
You can confirm your email (<%= @resource.unconfirmed_email %>) through the link below:
<% else %>
You can confirm your account through the link below:
<% end %>

<%= confirmation_url(@resource, confirmation_token: @token) %>
